id: drupalfr_website_drupal_version
label: 'Website Drupal version import'
migration_group: drupalfr
migration_tags:
  - drupalfr
dependencies:
  enforced:
    module:
      - drupalfr_migrate
  module:
    - migrate_plus
    - migrate_source_csv
    - taxonomy
migration_dependencies: {  }
source:
  plugin: csv
  path: profiles/drupalfr/modules/drupalfr_migrate/migrations/data/website_drupal_version.csv
  header_row_count: 1
  keys:
    - uuid
  column_names:
    -
      uuid: UUID
    -
      name: Name
    -
      weight: Weight
destination:
  plugin: 'entity:taxonomy_term'
process:
  vid:
    plugin: default_value
    default_value: references_drupal_versions
  uuid: uuid
  name: name
  weight: weight
